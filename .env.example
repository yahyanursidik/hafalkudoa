# Neon pooled connection string. Required for database-backed health checks and migrations.
DATABASE_URL=postgresql://USER:PASSWORD@ep-example.ap-southeast-1.aws.neon.tech/neondb?sslmode=require

# Optional. Enables structured development logging when set to development.
NODE_ENV=development
